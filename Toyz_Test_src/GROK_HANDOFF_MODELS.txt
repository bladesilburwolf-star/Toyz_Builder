2026-09-25 — Grok — GLB model loader

assets/models/*.glb (19 files from Blender / SerifSpriteToMesh):
  VRMan.glb, Booey, bat, ghost, slime1, nib, spiney, pacman, skull1,
  guard1, door1, Pede, boss_* (cactus, eyeballs, ghost, jellyfish,
  knight, pede, twinskull)

AssetBank
  - LoadModel(path) for GLB
  - modelForMob() maps kinds → GLB
  - drawModel() via DrawModelEx
  - Billboards remain fallback

In-game
  - Survival mobs prefer GLB, else sprite, else primitives
  - Third-person player uses VRMan.glb when present
  - MODEL_SCALE 0.55 / VRMAN_SCALE 0.65 (tweak if huge/tiny)

Run with cwd = project root so assets/models resolves.
console: [AssetBank] GLB ok: ... meshes=N

compileJava SUCCESS
